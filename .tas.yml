---
framework: mocha
tier: large
preRun:
  command:
    - npm install
preMerge:
  pattern:
    - "./**/*.spec.js"
postMerge:
  pattern: 
    - "./**/*.spec.js"
postRun:
  command:
    - node --version
version: 1.9
